{
  "name": "xlstatudf-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:docs": "node scripts/sync-docs.mjs",
    "dev": "npm run sync:docs && vite dev",
    "build": "npm run sync:docs && vite build",
    "preview": "vite preview",
    "deploy:aws": "powershell -ExecutionPolicy Bypass -File .\\scripts\\deploy-aws.ps1"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.17.1",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.20.2",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
